Fix copying stuff with context menu
Multiple selection copy/paste/cut
Icon state reordering + sort command
Handle delete hotkey in input block in nicer way
Readonly mode - diffs - Vscode doesn't support this well
Look into subrepo for the modified encoder
Autosplitting bigger pasted pngs into states/frames
Simplify all the data formats / encoder mess + performance pass on initial render
Improve css
Look into replacing esbuild with webpack/gulp so you can drop directly appending codicons
Run this through some more restrictive linter/prettier.